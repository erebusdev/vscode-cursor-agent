## Install the Cursor Agent CLI

```bash
curl https://cursor.com/install -fsS | bash
agent login
```

The extension talks to the CLI through Cursor's Agent Client Protocol (`agent acp`), so the same login, rules, MCP servers and skills you use in the terminal apply here.
